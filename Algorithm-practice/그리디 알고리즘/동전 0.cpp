#include <iostream>
#include <vector>

using namespace std;

/*
int main()
{
	int N;
	int K;
	vector<int> A;
	int sum = 0;
	int cnt = 0;

	cin >> N >> K;

	A.resize(N);

	for (int i = 0; i < N; i++)
	{
		cin >> A[i];
	}

	for (int i = N - 1; sum != K;)
	{
		if (A[i] <= K - sum)
		{
			sum += A[i];
			cnt++;
		}
		else
			i--;
	}

	cout << cnt;

	return 0;
}
*/