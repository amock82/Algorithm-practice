#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

/*
int main()
{
	int N;
	vector<int> P;

	int value = 0;
	int sum = 0;

	cin >> N;

	P.resize(N);
	
	for (int i = 0; i < N; i++)
	{
		cin >> P[i];
	}

	sort(P.begin(), P.end());

	for (int i = 0; i < N; i++)
	{
		value += P[i];
		sum += value;
	}

	cout << sum;

	return 0;
}
*/